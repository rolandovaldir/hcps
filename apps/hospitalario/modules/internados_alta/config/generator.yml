generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Internado
    theme:                 dojotheme
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          internado_internados_alta
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:    
        title: Historial de Personas Hopitalizadas dadas de Alta
        display: [matricula, nombre_completo, empresa, ubicacion, diagnostico]
        object_actions:
          visitar: {label: Visitar Paciente, action: visitar}     
        actions: {}
        batch_actions: {}
      filter:  
        display: [cama_id]
      form:    ~
      edit:    ~        
      new:     ~
